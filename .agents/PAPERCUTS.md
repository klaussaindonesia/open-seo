# Papercuts

Small, non-blocking friction in the repository itself — the kind that will
waste the next contributor's time too. Log it in the moment; review and fix
entries in a separate, user-requested cleanup pass.

This is not a completed-work log, a bug tracker, or a place for the agent's own
sandbox/shell/network hiccups. Never include secrets, credentials, personal
data, or sensitive paths.

## Open

- [ ] `2026-09-02T09:35:00Z` — `claude` — `GET /api/v1/blogs` **silently returns only 20 of ~745 rows when called without an `Authorization` header**, and the full corpus with one. Measured both ways this session: unauthenticated → 20 rows; `Authorization: Bearer <token>` → 745 rows; HTTP 200 both times, no error, no `total`, and not an edge cache (`cf-cache-status: DYNAMIC`, `?cb=` changes nothing). Two agents reached opposite conclusions about the same endpoint on the same day because one was authenticated and one was not. This is the highest-cost silent failure in the writer flow: coverage checks decide whether a topic is already published, so an unauthenticated listing reports ground the site already holds as an open gap and greenlights a near-duplicate. Either require the token (401 when absent), or return the same truncation marker the caller can see — a partial result that is indistinguishable from a complete one is worse than an error. Compounds the two-modes entry logged 2026-09-02T05:10:00Z.
- [ ] `2026-09-02T09:40:00Z` — `claude` — A **repealed regulation reads `Berlaku` in the corpus while the instrument that repealed it is absent from the corpus entirely**, so the repeal is undiscoverable from Klaussa data alone. Permen PUPR 14/2021 (PPPSRS, id `296158`) is revoked by **Permen PKP 4/2025 Pasal 100** (ditetapkan 17 April 2025, verified in the official JDIH PDF under BAB XVIII Ketentuan Penutup), yet `296158` still reports `Berlaku` and `title-search` does not surface Permen PKP 4/2025 under any phrasing tried (its exact title, its subject, `Permen PKP 4 tahun 2025`) — while sibling Permen PKP 5/2025, 9/2025 and 10/2025 are all present at ids ~311107-311217. A writer following the brief's own source hierarchy would have published an article built on a dead instrument; only an independent JDIH check caught it. The existing status-field entry (2026-09-01T08:50:00Z) covers a stale flag on a document that is present; this is worse, because no amount of corpus reading reveals the successor. Backfill 2025 Permen PKP ingests, and until then the writer brief must say that a `Berlaku` flag on a Permen is not evidence when the ministry was reorganised (PUPR → PKP in 2024) — check the new ministry's JDIH directly.
- [ ] `2026-09-02T08:20:00Z` — `claude` — `GET /api/v1/laws/markdown/<id>` **prints an article's body before its own `## Pasal N` heading** when the article opens a new BAB, so the text reads as a continuation of the previous article. In Permen ATR/BPN 3/2023 (doc `279872`) the repeal clause for Permen ATR/BPN 1/2021 sits under `## BAB IX KETENTUAN PENUTUP` between the text of Pasal 45 and an empty `## Pasal 46` heading — so the obvious reading attributes the repeal to Pasal 45, and that wrong citation reached a live published post before it was caught and patched. The same response also encodes newlines as **literal `\n` escape sequences** rather than real line breaks, so a naive `split(/\n## Pasal/)` finds zero articles and an agent that hits this concludes the document has no article structure. Two cheap fixes: emit real newlines, and attach each `BAB`/`Bagian` heading to the article that follows it (or expose a structured `pasal` array — the field already exists on the `title-search` record but comes back `null`). Until then, `BRIEF.md` should say that an article's number must be confirmed from the heading *below* its text when that text follows a BAB heading, and that pivotal citations need a cross-check against official JDIH.
- [ ] `2026-09-02T14:10:00Z` — `claude` — `POST /api/v1/blogs` **silently discards a `slug` sent in the body** and derives its own from the title, the same way it already drops `meta_description`. Measured this session: posting `"slug": "njop-adalah-dasar-hukum-dan-cara-mengecek"` returned `njop-adalah-dasar-hukum-dan-cara-mengecek-njop-properti-anda`, and `"slug": "pbb-p2-setelah-uu-hkpd-tarif-dan-keberatan-njop"` returned `pbb-p2-setelah-uu-hkpd-tarif-cara-hitung-dan-keberatan-njop` — no error, no warning, HTTP 200 both times. This bites cluster jobs specifically: the wave briefs assign an exact slug per post and the posts interlink by slug, so a writer that publishes the pillar and then hardcodes the *assigned* slug into its support post ships a dead internal link on both pages. The fix is a service-role `PATCH /rest/v1/blogs?id=eq.<id>` on `slug` before the publish flip, which is exactly the workaround already documented for `meta_description`. Either honour a supplied `slug` (or 4xx when it is rejected), or extend the `BRIEF.md` note that currently only warns about `meta_description` to say the POST endpoint drops `slug` too and both need their own PATCH plus a read-back.
- [ ] `2026-09-02T09:05:00Z` — `claude` — `api.klaussa.com` answers **every unrecognised route with `401 {"detail":"Bearer token is required"}`** instead of a 404, so a wrong auth path is indistinguishable from bad credentials. Measured: `POST /api/v1/auth/sign-in` → 200 with a session; `POST /api/v1/auth/login`, `/auth/signin`, `/auth/token` and even `/api/v1/nope` → all `401 Bearer token is required`. An agent handed only `WRITER_EMAIL`/`WRITER_PASSWORD` and told to use "the writer-bot bearer token" burns several probes deciding whether the credentials are stale before it discovers the path is hyphenated (`sign-in`, and the token is at `session.access_token`). Return 404 for unknown routes, and name the exact sign-in route in the correction brief the way `seo-geo-cron/prompts/seo.md:174` already does for the writer job.
- [ ] `2026-09-02T05:10:00Z` — `claude` — `GET /api/v1/blogs` has **two incompatible modes** and nothing signals which you are in, so agents reach opposite conclusions about the same endpoint. Measured: `/blogs` → 702 rows; `/blogs?per_page=100` → **702** (`per_page` silently ignored); `/blogs?summary=true` → **20**; `/blogs?summary=true&per_page=100&include_total=true` → 100 with `total: 702`. So `summary=true` is what switches on pagination, and `page`/`per_page`/`include_total` are silently dropped without it. This session produced three contradictory reports from three writers because of it — one collected 7,513 rows across 11 identical "pages", another concluded the corpus held 20 posts and duplicate-checked candidates against 2.9% of it. Either honour the paging params in both modes or reject them with a 4xx when they cannot apply; silently ignoring a paging parameter is the failure mode that costs the most.
- [ ] `2026-09-02T08:20:00Z` — `claude` — `GET /api/v1/blogs` **silently returns only the first 20 of 700 published posts** with no `total`, no `next`, and no pagination metadata in the body, so a caller that does not already know the corpus size reads the response as the whole corpus. `per_page` *is* honoured but caps at 100 (`per_page=1000` returns a 422 with `{"le": 100}`), and `page` paginates normally — the full corpus needs 7 calls. Anything that dedupes a proposed blog topic against "the existing posts" on the bare endpoint is checking against 2.9% of them and will greenlight duplicates of ground the site already holds. `seo-geo-cron/prompts/seo.md` documents only `POST /api/v1/blogs` (creation) and `_common.md` only `GET /blogs/{blog_id}`, so the listing endpoint's paging is written down nowhere. Either return an envelope with `total`/`next` (and default `per_page` to the cap), or state the `?page=&per_page=100` loop in `_common.md` next to the existing blog-API notes. Related: `api.klaussa.com` 403s Python `urllib`'s default user-agent, so the loop has to be `curl` (already logged 2026-08-27).
- [ ] `2026-09-01T08:50:00Z` — `claude` — The corpus `status` field reports a flat **`Tidak Berlaku` for laws that are only partially repealed**, because BPK sets it on the mere existence of a `dicabut dengan` edge regardless of that edge's scope. UU 6/1996 tentang Perairan Indonesia (id `117834`) reads `Tidak Berlaku` although UU 32/2014 Pasal 72 repeals exactly one ayat of it (Pasal 24 ayat (3), badan koordinasi) and the rest is plainly in force — BPK's own detail page even prints the qualifier "Mencabut ketentuan mengenai pembentukan badan koordinasi sebagaimana diatur dalam Pasal 24 ayat (3)" directly under the `Dicabut dengan` heading, but `title-search` surfaces only the coarse flag. The same dataset errs the other way too: Perpu 4/1960 (id `150571`) reads `Berlaku` though UU 6/1996 repealed it outright. One writer already deleted a correct UU 6/1996 citation from two live posts on the strength of this flag. Carry the repeal-scope qualifier through into the API response (or add a `dicabut_sebagian` boolean), and until then have `seo-geo-cron/prompts/seo.md` and the correction brief say: `status` is a hint, not a holding — before dropping a citation as repealed, read the repealing instrument's Ketentuan Penutup and confirm it repeals the whole law.
- [ ] `2026-09-01T08:45:00Z` — `claude` — `title-search`'s identifier parser **drops the region token**, so regional-regulation lookups silently return the wrong province. `q="Peraturan Gubernur Provinsi Daerah Khusus Ibukota Jakarta Nomor 30 Tahun 2016"` parses to `{bentuk: Pergub, nomor: 30, tahun: 2016}` with `lokasi` absent, and returns 12 Pergub 30/2016 from Jawa Tengah, Riau, Sumsel, Kalteng and others — the DKI one is not reliably first, and adding a topical word (`... kantor virtual`) reorders the list without filtering it. A writer verifying a Pergub/Perda citation who takes the top hit will cite another province's instrument and never notice, because every row looks like a correct match on bentuk/nomor/tahun. The `lokasi` field is already populated on the law records, so make the parser extract the region and filter on it, or surface `lokasi` in the ranked output. Until then the correction brief should say: for any Pergub, Perda, Perwali or Perbup, match `lokasi`/`name` on the region before trusting a hit, and confirm against the row's `raw_url` on peraturan.bpk.go.id.
- [ ] `2026-09-01T08:40:00Z` — `claude` — Recent corpus documents (Permenkum 49/2025 id `311184`, PP 30/2026 id `313514`) 404 on `/api/v1/laws/markdown/<id>` — no markdown has been ingested into R2 — so the only route to their text is `/api/v1/laws/pdf/<id>` plus local `pdftotext`. Worse, those BPK PDFs carry a **corrupted text layer that fails silently**: PP 30/2026 extracts `PEMEzuNTAH`/`BERI.AKU`/`Paj al<` and `50.ooo,oo` for `50.000,00`, and Permenkum 49/2025 renders its promulgation date and Berita Negara number as single junk glyphs (`Д`, `Ж`) while the rest of the page reads fine. A writer grepping for a tariff or an LN/BN number gets a plausible-looking wrong answer or a blank, with nothing signalling the substitution. Backfill markdown for post-2024 ingests, and until then have `seo-geo-cron/prompts/seo.md` and the correction brief say: PDF-only docs must be read as prose, never grepped for exact figures, and any number pulled from one needs a second reading of its surrounding sentence.
- [ ] `2026-08-31T05:15:00Z` — `claude` — `/api/v1/laws/available`'s `hierarchies` filter needs the exact DB `bentuk` string: `Peraturan Pemerintah (PP)` works, `Peraturan Pemerintah` silently returns `total: 0` rather than an error or a hint. That is the fourth way this corpus API answers "zero results" when the data is present (see also identifier-only `title-search` queries, the transient `'coroutine' object is not iterable`, and `title-search` simply failing to surface a present document). The bentuk map lives in `src/law-search/utils/hierarchy.ts`. Either accept the short form or return a did-you-mean; a silent zero is the worst possible answer because callers act on it as fact.
- [ ] `2026-08-31T04:30:00Z` — `claude` — Fanning out ~20 blog-writing agents at once makes `POST /api/v1/auth/sign-in` return **HTTP 429**; one writer's first sign-in was rejected and succeeded on a retry minutes later. A 429 here is easy to misread as bad credentials, since the same endpoint also 403s behind Cloudflare for a non-browser UA. Give the writer flow an explicit retry with backoff on 429 and say in `BRIEF.md` that a rejected sign-in during a large batch is rate limiting, not an auth failure.
- [ ] `2026-08-31T02:10:00Z` — `claude` — `GET /api/v1/laws/title-search` can return a **spurious `total: 0`** rather than an error status: the body comes back as `{"path":"service_error","error":"'coroutine' object is not iterable","total":0}`. A caller checking only `total` reads that as "this law is not in the corpus" and works around a gap that does not exist — one blog-writing agent hit it twice in a row mid-session and it recovered on retry with no change to the query, so it is transient and not query-dependent. The underlying fault looks like an unawaited coroutine in the search path, which is a backend bug worth fixing on its own; until then, callers must check `path`/`error` and retry rather than trusting `total: 0`. This compounds the existing identifier-query trap logged below.
- [ ] `2026-08-30T16:40:00Z` — `claude` — The corpus `GET /api/v1/laws/title-search?q=` endpoint returns `total: 0` for a pure identifier query — `"UU 40 tahun 2007"` yields nothing and the response sets `is_identifier_query: true` — so a writer looking up a statute the obvious way concludes it is absent from the corpus when it is present. You must include topical words (`"undang-undang perseroan terbatas 2007"`) to get the hit. Cost one blog-writing agent two dead lookups before it guessed the workaround. Either make identifier queries resolve, or state the rule in `seo-geo-cron/prompts/seo.md` and in the writer brief. Related: some ids resolve in title-search but 404 on `/api/v1/laws/markdown/<id>` (KUHPerdata id 999001 — "not found in R2"); `/api/v1/laws/pdf/<id>` worked as a fallback for one such doc.
- [ ] `2026-08-30T14:20:00Z` — `claude` — Timing the `/peraturan/jenis/<bentuk>` hubs with back-to-back cache-busted requests exhausts the backend connection pool: the first URLs in the run return real latencies and the tail returns *fast* 503s (0.2–0.4s), so the run reads as "these hubs are down" when the measurement is generating the failures. A second pass 20s later came back almost all 503, while the same URLs sampled one at a time 30s apart were all 200. Space hub samples out, and read a sub-second 503 as pool exhaustion rather than a slow query — a slow query presents as a multi-second 200. (be#1049's async fix is merged to staging but not yet deployed to production, so the pool is still the pre-fix one.)
- [ ] `2026-08-30T11:05:00Z` — `claude` — The OpenSEO project for klaussa.com defaults to `locationCode 2840` (United States) / `languageCode "en"`, so every rank number that falls back to the project default — `get_serp_results`, `get_ranked_keywords`, the rank tracker — measures an Indonesian-language site against a US/English SERP. Two clusters that the tracker reported at positions 63 and 84 sat at ~11 and ~9 when re-queried with `locationCode 2360` / `languageCode "id"`, so a job brief was built on a "content gap" that did not exist. Fix the project's default market, and until then have `seo-geo-cron/prompts/seo.md` require an explicit `locationCode 2360` + `languageCode "id"` on every OpenSEO call.
- [ ] `2026-08-29T09:20:00Z` — `claude` — A correction written to a published blog row stays invisible on the canonical `/blog/<slug>` URL for up to 24h: `functions/blog/[slug].ts` stores rendered HTML in `caches.default` with `CACHE_TTL_SECONDS = 86_400`, and the only busting paths are bumping `CACHE_VERSION` (needs a deploy) or a Cloudflare URL purge, which the Cloudflare token these jobs are handed cannot do (`purge_cache` returns code 10000 while `GET /zones` succeeds). Appending any query string re-renders and shows the fix, so a correct write looks like a failed one. Give content-fix jobs a purge-capable token, or document the `?cb=` re-render check in `seo-geo-cron/prompts/seo.md`.
- [ ] `2026-08-27T05:12:00Z` — `claude` — `seo-geo-cron/run.sh` does `cd "$SCRIPT_DIR/.."` before invoking claude but `pilot-run.sh` does not, so the repo-root-relative paths the prompts use (`seo-geo-cron/data/actions.sqlite`, `seo-geo-cron/data/budget.json`) resolve to different places depending on which runner launched the job — a piloted run can silently create a second actions.sqlite. Add the same `cd` to `pilot-run.sh`.
- [ ] `2026-08-27T05:12:00Z` — `claude` — `api.klaussa.com` rejects Python `urllib`'s default user-agent with a bare 403 while accepting `curl` with an identical body and headers, so a Python helper against the blog API fails looking like an auth problem when the token is fine. `seo-geo-cron/prompts/seo.md` documents only the curl form; worth stating outright that the edge filters by user-agent and non-curl clients must set one.
- [ ] `2026-08-18T03:06:44Z` — `claude` — Changing an MCP tool's `outputSchema` while the dev server hot-reloads makes in-flight MCP sessions reject the tool's own (already billed) results — clients validate against the schema cached at connect time, surfacing as "must NOT have additional properties". Note in the MCP dev docs/skill: reconnect the MCP session after any output-schema change before re-testing live.
- [ ] `2026-08-05T20:59:09Z` — `codex` — The documented `pnpm seed:rank-tracking` command fails before opening local D1 because `scripts/seed-rank-tracking.ts` imports the provider-aware `src/db/schema` barrel and plain `tsx` cannot load the resulting `cloudflare:workers` URL. Keep the seed script on dialect-local schema imports or run it through a Workers-compatible execution path. (Workaround: seed via raw SQL with `wrangler d1 execute DB --local`.)
- [ ] `2026-08-01T16:28:36Z` — `claude` — web's pinned wrangler 4.71.0 fails `kv namespace create` with a bare "Authentication error [code: 10000]" even though the OAuth token has workers_kv write scope; wrangler@4.118.0 succeeds with identical auth. Fix: bump wrangler in web/package.json.
- [ ] `2026-07-20T20:08:28Z` — `claude` — In a fresh git worktree, `oxlint --type-aware` crashes with `Cannot find module '@oxlint/binding-darwin-arm64'` — the platform-specific optional dep is missing from the worktree's node_modules while tsc/prettier work fine, and plain `pnpm install` reports up-to-date without restoring it; `pnpm install --force` (~22s) fixes it. Worth making the worktree-setup hook (or a documented step) run the forced install so lint doesn't die on fresh worktrees.
- [ ] `2026-07-19T04:06:52Z` — `codex` — `pnpm --dir web build` fails with `vite: command not found` when `web/node_modules` is absent, despite the root toolchain being installed. Document or enforce the package-local install required before validating the `web/` subpackage.
- [ ] `2026-07-19T02:55:56Z` — `claude` — Adding a docs folder under `web/content/docs` whose `meta.json` lists an `[Overview](...)` link renders a duplicated, double-highlighted sidebar entry, because the folder-index strip in `web/src/lib/source.ts` (`transformPageTree.folder`) is a per-folder-name allowlist. Derive it from the meta convention (or strip the index for all folders) so new sections don't need a hidden source.ts edit.
- [ ] `2026-07-14T01:28:30Z` — `claude` — Regenerating the lockfile (adding or moving a dep) makes `pnpm install` re-run the `minimumReleaseAge` gate on transitive peers already pinned at that exact version (`mysql2`, `sql-escaper`, `@aws-sdk/credential-providers`), failing the install even though nothing about them changed. `pnpm install --config.minimumReleaseAge=0` — then confirm the lockfile diff stays version-neutral — unblocks it; worth documenting that regen step so the gate doesn't re-block already-pinned versions.
- [ ] `2026-07-10T21:28:46Z` — `codex` — `pnpm --dir badseo run typecheck` works through the root toolchain but `pnpm --dir badseo run build` can't find Vite because `badseo/node_modules` is absent. Document or enforce the package-local install before validating the `badseo/` subpackage.
- [ ] `2026-07-10T21:32:10Z` — `codex` — Formatting the `badseo/` workspace with `pnpm exec prettier` fails because Prettier is only available from the repository root. Document the root-only formatter command or expose a workspace-local formatting script.

## Resolved

Move fixed entries here, mark them checked, and append the resolving date or commit.

- [x] `2026-08-25T13:38:51Z` — `claude` — `update_project_context`'s `appendResearchLog.summary` silently caps at 1000 characters, but `seo-geo-cron/prompts/_common.md` just says to append "a one-line entry". Fixed 2026-08-25: `_common.md` now states the 1000-char cap next to the instruction.
- [x] `2026-08-25T12:35:00Z` — `claude` — `seo-geo-cron/prompts/geo.md` told the GEO job to run "all 20 prompts" in month 1 while `data/prompts.json`/`data/budget.json` both say 22. Fixed 2026-08-25: `geo.md` now cites 22 and defers to `budget.json` if the count ever drifts again.

## badseo harness vs `wrangler dev`: sitemap emits badseo.dev locs locally

`badseo/scripts/run-audit.ts` against a local `wrangler dev --port 8787` fails 4
sitemap-dependent checks (orphan page, 500, 403, duplicate-content) with
NOT CRAWLED: wrangler dev adopts the `badseo.dev` custom-domain route as the
host the worker sees, so `/sitemap.xml` emits `http://badseo.dev/...` locs that
the crawler's same-origin filter drops. Run it as
`wrangler dev --port 8787 --local-upstream "localhost:8787"` (after
`vite build`). Also: `pnpm --filter badseo audit` fails with
"Unknown option: 'recursive'" from the repo root — badseo is its own pnpm
workspace, not a root workspace member; use `npx tsx badseo/scripts/run-audit.ts`.
