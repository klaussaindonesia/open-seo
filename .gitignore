.codex/
node_modules
package-lock.json
yarn.lock
.dev.vars
dist/
dist-sourcemaps/
.wrangler
/tmp

.output/
.tanstack/
.DS_Store
.cache
.env
.env.
.env.*
!.env.example
!.env.preview.example
!.env.production.example
!.env.selfhost.example
.vercel
.output
.nitro
/build/
/api/
/server/build
/public/build# Sentry Config File
.env.sentry-build-plugin
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.tanstack
.logs/
**/.pnpm-store/

# Localflare generated files
.localflare/

# Local Claude config (skills are shared)
.claude/*
!.claude/skills/

# Alchemy local state + bundle artifacts (SaaS deploys)
.alchemy/

# seo-geo-cron: local GEO history + logs, the klaussa_fe workspace clone,
# and the local secrets file (writer bot creds, blog-review-email HMAC secret)
seo-geo-cron/data/*.sqlite
seo-geo-cron/data/*.log
seo-geo-cron/klaussa_fe-workspace/
seo-geo-cron/.env.local
